name: Update recent publications from PubMed

on:
  schedule:
    # 每天 UTC 19:00 执行一次
    - cron: "0 19 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme-with-pubmed:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Update README with PubMed RSS
        uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "https://pubmed.ncbi.nlm.nih.gov/rss/search/1lyR11GsxK1bMA3jTm7Uhi20O_g2bcbLaEf-itlqG1dZLlVEGt/?limit=15&utm_campaign=pubmed-2&fc=20251114012738"
          max_post_count: 10
          comment_tag_name: "BLOG-POST-LIST"
          date_format: "yyyy"
          # 从 PubMed RSS 拿作者和期刊
          custom_tags: "authors/dc:creator/,journal/dc:source/"
          # 注意：用 $newline 在每条目前后加换行
          template: "$newline- $authors. **$title**. *$journal*. $date. [PubMed]($url)$newline"
          # JS：清洗作者，最多保留前 5 个，>5 时加 “et al.”，并把 Xinsheng Wu 加粗
          item_exec: |
            if (post.authors) {
              // 去掉多余空格和末尾的点号
              let txt = post.authors.replace(/\s+/g, ' ').trim();
              txt = txt.replace(/\.*\s*$/, '');

              // PubMed 作者一般用逗号分隔
              let parts = txt.split(',').map(a => a.trim()).filter(Boolean);

              // 把包含 Xinsheng + Wu 的作者统一加粗
              parts = parts.map(a => {
                const norm = a.replace(/\s+/g, ' ').toLowerCase();
                if (norm.includes('xinsheng') && norm.includes('wu')) {
                  return '**Xinsheng Wu**';
                }
                return a;
              });

              // 如果作者多于 5 个，只保留前 5 个 + et al.
              if (parts.length > 5) {
                parts = parts.slice(0, 5);
                parts.push('et al.');
              }

              post.authors = parts.join(', ');
            }
          commit_message: "chore: update recent publications from PubMed"
